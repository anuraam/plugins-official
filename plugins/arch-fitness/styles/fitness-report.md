# Architecture Fitness Report Template

The lead agent / `fitness-evaluator` must follow this format when compiling the fitness report. Fill every header field with concrete values — no placeholders.

Task mode posts this markdown as a comment (GitHub issue or Azure DevOps work item). Chat mode prints it. Generic remotes write it to `arch-fitness-report.md`.

---

## Architecture Fitness Report

**Verdict:** `FIT` | `DRIFTING` | `AT RISK`
**Trigger:** [Issue #`<n>`: `<title>`] or [Work Item #`<id>`: `<title>`] or [Chat]
**Repository:** `<owner/repo>` or `<org/project/repo>`
**Scope:** `<pr #N | branch name | merged PRs YYYY-MM-DD..YYYY-MM-DD>`
**Files in scope:** `<count>`
**Constraints source:** `existing` | `drafted` | `updated`
**Constraints ratification:** `ratified` | `pending` | `mixed`
**Docs PR:** `<url>` or `none (docs already up to date)`
**Max findings:** `<n>`
**Findings reported:** `<n>`

> When **Constraints ratification** is `pending` or `mixed`, treat findings against `status: proposed` constraints as advisory until the docs PR is reviewed and merged.

---

### Summary

[2–4 sentences: overall fitness posture, whether drift is systemic or local, whether docs were bootstrapped/updated in this run, and the single most important action for the team.]

---

### Top improvements

> Up to `Max findings` items, ranked by severity × blast radius × recurrence.

| # | Constraint | Severity | Recurrence | One-line improvement |
|---|---|---|---|---|
| 1 | `ARCH-00N` — title | high | 7/12 PRs | Stop calling the DB from controllers |
| 2 | `ARCH-00M` — title | medium | 2 files | Move shared DTO out of the UI package |

*(If none: "No architecture fitness issues identified in the scoped changeset.")*

---

### Finding details

For each reported finding, use this shape:

#### 1. `<short title>`

- **Constraint:** `ARCH-00N` — `<title>` (`severity`)
- **Evidence:**
  - `path/to/file.ext:12-40` (PR #`<n>` when multi-PR)
  - …
- **Why it matters:** [architecture impact — boundary break, dependency inversion, coupling, etc.]
- **Remediation:** [concrete, scoped next step]
- **Recurrence:** [e.g. `7 of 12 merged PRs in window` or `single occurrence`]

---

### Scope summary

| Field | Value |
|---|---|
| Kind | `pr` / `branch` / `merged-window` |
| Value | … |
| PR count | … |
| Window | `start..end` or `n/a` |
| Focus areas | … or `none` |
| Skip areas | … or `none` |

---

### Docs activity

| Action | Detail |
|---|---|
| Discovery | [what was found / not found] |
| Bootstrap / update | [files touched, or "no changes"] |
| Docs PR | [url or none] |
| Proposed constraint IDs | [list or none] |

---

### Next steps

1. [If docs PR open] Review and merge the docs PR; ratify proposed constraints.
2. Address the top finding(s) in order of rank.
3. Re-run `/arch-fitness` on the same scope after fixes to confirm the verdict moves toward `FIT`.

---

_Generated by the `arch-fitness` plugin._
